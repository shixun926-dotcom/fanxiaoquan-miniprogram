# 饭小圈 · 兑换码

以下 3 个兑换码为系统初始兑换码，由云端 `seed` 在 `codes` 集合为空时自动创建（固定值，与 `cloudfunctions/api/index.js` 的 `SEED_CODES` 保持一致）。

| 兑换码 | 兑换获得经验 | 使用次数上限 |
| --- | --- | --- |
| `FXQ8F3K2Q` | +30 经验 | 100 次 |
| `FXQ7J9M4X` | +30 经验 | 100 次 |
| `FXQ2T5N8V` | +30 经验 | 100 次 |

## 使用规则

- 兑换入口：「我的」页 → 兑换码 → 输入兑换码（不区分大小写）→ 兑换，成功立即 +30 经验
- 每个兑换码最多使用 **100 次**；每个账号对同一兑换码**只能兑换一次**
- 兑换码不存在 / 已使用完 / 已兑换过，均有明确提示

## 运营维护

- 次数用尽后：云开发控制台 → 数据库 → `codes` 集合，将该码文档的 `usedCount` 重置为 `0`（或删除该文档，下次小程序启动由 `seed` 重新创建）
- 调整经验值 / 次数上限：`cloudfunctions/api/index.js` 顶部 `CODE_EXP`（默认 30）/ `CODE_MAX_USES`（默认 100），改后需重新部署云函数
- 兑换记录存 `redeems` 集合（`_id = openid:code`），用于审计每个账号的兑换情况
